version: "3"

services:
  node-exporter:
    image: quay.io/prometheus/node-exporter
    container_name: node-exporter
    volumes:
        - /:/host:ro
    hostname: "macbook pro 2017"
    restart: always
    ports:
        - "9100:9100"

    
