version: "3"
services:
    node-exporter:
        image: quay.io/prometheus/node-exporter
        command: "--web.listen-address=:9200"
        container_name: node-exporter
        volumes:
            - /:/host:ro
        hostname: dudu
        restart: always
        ports:
            - 9200:9200
